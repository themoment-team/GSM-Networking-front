import { MyInfoUpdate } from '@/pageContainer';

import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '내 정보 수정',
};

const MyInfoUpdatePage = () => <MyInfoUpdate />;

export default MyInfoUpdatePage;
