import styled from 'styled-components';

export const Layout = styled.main`
  width: 600px;
  height: 100%;
  margin: 0 auto;
  position: relative;

  @media (max-width: 600px) {
    width: 100%;
  }
`;
